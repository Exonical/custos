# Configuration

Generated by tools/docsconfig — do not edit by hand.

## Sources and precedence

1. Built-in defaults (`config.Default()`).
2. YAML config file (`--config` / `CUSTOS_CONFIG`), overlaid
   field-by-field.
3. Environment variables `CUSTOS_<SECTION>__<KEY>` (`__` separates
   nesting levels, keys uppercased). `worker.kinds` maps cannot be set
   via env.

Secret fields (`Secret` type) also accept `<NAME>_FILE` pointing at a
file whose trimmed contents are the value; setting both the variable and
its `_FILE` variant is an error. Secrets are never logged or serialized.

## Fail-closed rules

Validation aborts startup (`config.invalid`) when:

- `server.tls.mode=required` (the default) without
  `cert_file`/`key_file`; `mode=disabled` only with `dev_mode`.
- `database.ssl_mode=disable` only with `dev_mode`; the URL must not
  contain `sslmode`/`sslrootcert`/`sslcert`/`sslkey` (TLS is
  configured via `database.ssl_mode`, never the URL).
- `telemetry.insecure` only with `dev_mode`.
- Without `dev_mode`: `auth.oidc` issuer (https URL), client_id and
  at least one audience are required.
- CORS origins must be absolute https origins (http only in dev_mode);
  no `*`; `server.trusted_proxies` must be CIDRs.

## Keys

| Key | Type | Default | Env | Description |
| --- | ---- | ------- | --- | ----------- |
| `auth` | section | `` | `CUSTOS_AUTH` | OIDC authentication |
| `auth.oidc` | section | `` | `CUSTOS_AUTH__OIDC` | OIDC relying-party settings |
| `auth.oidc.accepted_token_types` | slice | `at+jwt,JWT,` | `CUSTOS_AUTH__OIDC__ACCEPTED_TOKEN_TYPES` | Permitted JOSE typ header values (empty string = typ absent) |
| `auth.oidc.allowed_algorithms` | slice | `RS256,ES256` | `CUSTOS_AUTH__OIDC__ALLOWED_ALGORITHMS` | Permitted JWS algorithms |
| `auth.oidc.audiences` | slice | `` | `CUSTOS_AUTH__OIDC__AUDIENCES` | Accepted token audiences |
| `auth.oidc.ca_file` | string | `` | `CUSTOS_AUTH__OIDC__CA_FILE` | PEM CA bundle for the OIDC issuer's TLS (empty = system roots) |
| `auth.oidc.claims` | section | `` | `CUSTOS_AUTH__OIDC__CLAIMS` | Claim names mapped onto Principal fields |
| `auth.oidc.claims.email` | string | `email` | `CUSTOS_AUTH__OIDC__CLAIMS__EMAIL` | Claim carrying the email address |
| `auth.oidc.claims.groups` | string | `groups` | `CUSTOS_AUTH__OIDC__CLAIMS__GROUPS` | Claim carrying group memberships (string array) |
| `auth.oidc.claims.name` | string | `name` | `CUSTOS_AUTH__OIDC__CLAIMS__NAME` | Claim carrying the display name |
| `auth.oidc.claims.subject` | string | `sub` | `CUSTOS_AUTH__OIDC__CLAIMS__SUBJECT` | Claim carrying the subject (default sub) |
| `auth.oidc.client_id` | string | `` | `CUSTOS_AUTH__OIDC__CLIENT_ID` | OIDC client identifier |
| `auth.oidc.client_secret` | secret | `` | `CUSTOS_AUTH__OIDC__CLIENT_SECRET` | OIDC client secret |
| `auth.oidc.clock_skew` | duration | `30s` | `CUSTOS_AUTH__OIDC__CLOCK_SKEW` | Allowed issuer clock skew |
| `auth.oidc.discovery` | bool | `true` | `CUSTOS_AUTH__OIDC__DISCOVERY` | Fetch issuer .well-known/openid-configuration for jwks_uri |
| `auth.oidc.issuer` | string | `` | `CUSTOS_AUTH__OIDC__ISSUER` | OIDC issuer URL (https, no query/fragment) |
| `auth.oidc.jwks_cache_ttl` | duration | `1h0m0s` | `CUSTOS_AUTH__OIDC__JWKS_CACHE_TTL` | Background JWKS refresh cadence |
| `auth.oidc.jwks_refresh_min_interval` | duration | `30s` | `CUSTOS_AUTH__OIDC__JWKS_REFRESH_MIN_INTERVAL` | Min interval between on-demand JWKS refreshes (unknown kid) |
| `auth.oidc.jwks_uri` | string | `` | `CUSTOS_AUTH__OIDC__JWKS_URI` | JWKS endpoint override; required when discovery=false |
| `auth.oidc.max_token_lifetime` | duration | `24h0m0s` | `CUSTOS_AUTH__OIDC__MAX_TOKEN_LIFETIME` | Max exp-iat; requires iat when > 0 |
| `auth.oidc.redirect_url` | string | `` | `CUSTOS_AUTH__OIDC__REDIRECT_URL` | OIDC redirect URL for the frontend |
| `auth.oidc.required_scopes` | slice | `` | `CUSTOS_AUTH__OIDC__REQUIRED_SCOPES` | Scopes every access token must carry (many IdPs omit openid; default empty) |
| `database` | section | `` | `CUSTOS_DATABASE` | PostgreSQL connection pool |
| `database.app_role` | string | `` | `CUSTOS_DATABASE__APP_ROLE` | Runtime role granted least-privilege DML by migrate up; empty = skip |
| `database.connect_timeout` | duration | `5s` | `CUSTOS_DATABASE__CONNECT_TIMEOUT` | Connection establishment timeout |
| `database.max_conns` | int32 | `16` | `CUSTOS_DATABASE__MAX_CONNS` | Maximum pool connections |
| `database.min_conns` | int32 | `2` | `CUSTOS_DATABASE__MIN_CONNS` | Minimum pool connections kept warm |
| `database.require_scram` | bool | `true` | `CUSTOS_DATABASE__REQUIRE_SCRAM` | Fail startup unless server password_encryption is scram-sha-256 |
| `database.ssl_mode` | string | `verify-full` | `CUSTOS_DATABASE__SSL_MODE` | disable\|require\|verify-ca\|verify-full (disable is dev only) |
| `database.statement_timeout` | duration | `30s` | `CUSTOS_DATABASE__STATEMENT_TIMEOUT` | Per-statement server-side timeout |
| `database.tls` | section | `` | `CUSTOS_DATABASE__TLS` | Client-side TLS material for the database connection |
| `database.tls.cert_file` | string | `` | `CUSTOS_DATABASE__TLS__CERT_FILE` | PEM client certificate (mTLS); requires key_file |
| `database.tls.key_file` | string | `` | `CUSTOS_DATABASE__TLS__KEY_FILE` | PEM client key (mTLS); requires cert_file |
| `database.tls.root_ca_file` | string | `` | `CUSTOS_DATABASE__TLS__ROOT_CA_FILE` | PEM CA file for verify-ca/verify-full (empty = system roots) |
| `database.url` | secret | `` | `CUSTOS_DATABASE__URL` | postgres:// DSN; TLS params are set via ssl_mode, never in the URL |
| `dev_mode` | bool | `false` | `CUSTOS_DEV_MODE` | Permit insecure settings for local development; never enable in production |
| `log` | section | `` | `CUSTOS_LOG` | Structured logging |
| `log.format` | string | `json` | `CUSTOS_LOG__FORMAT` | json\|text |
| `log.level` | string | `info` | `CUSTOS_LOG__LEVEL` | debug\|info\|warn\|error |
| `metrics` | section | `` | `CUSTOS_METRICS` | Separate Prometheus metrics listener |
| `metrics.enabled` | bool | `true` | `CUSTOS_METRICS__ENABLED` | Expose GET /metrics on the metrics listener |
| `metrics.listen` | string | `:9090` | `CUSTOS_METRICS__LISTEN` | Listen address for the metrics listener |
| `metrics.tls` | section | `` | `CUSTOS_METRICS__TLS` | TLS settings for the metrics listener |
| `metrics.tls.cert_file` | string | `` | `CUSTOS_METRICS__TLS__CERT_FILE` | PEM certificate chain file (required when mode=required) |
| `metrics.tls.client_ca_file` | string | `` | `CUSTOS_METRICS__TLS__CLIENT_CA_FILE` | PEM CA file for verifying client certificates |
| `metrics.tls.key_file` | string | `` | `CUSTOS_METRICS__TLS__KEY_FILE` | PEM private key file (required when mode=required) |
| `metrics.tls.mode` | string | `required` | `CUSTOS_METRICS__TLS__MODE` | required (default) \| disabled (dev only) \| upstream |
| `metrics.tls.require_client_cert` | bool | `false` | `CUSTOS_METRICS__TLS__REQUIRE_CLIENT_CERT` | Require and verify client certificates (mTLS) |
| `secrets` | section | `` | `CUSTOS_SECRETS` | Secret-provider settings (docs/secrets.md) |
| `secrets.file_roots` | slice | `C:\custos\secrets` | `CUSTOS_SECRETS__FILE_ROOTS` | Allow-listed absolute roots for the file secret provider |
| `server` | section | `` | `CUSTOS_SERVER` | Public HTTP API listener |
| `server.cors` | section | `` | `CUSTOS_SERVER__CORS` | Cross-origin allow-list |
| `server.cors.allowed_origins` | slice | `` | `CUSTOS_SERVER__CORS__ALLOWED_ORIGINS` | Exact-match allowed origins (no wildcards) |
| `server.idle_timeout` | duration | `2m0s` | `CUSTOS_SERVER__IDLE_TIMEOUT` | Keep-alive idle connection timeout |
| `server.listen` | string | `:8443` | `CUSTOS_SERVER__LISTEN` | Listen address (host:port) for the API |
| `server.max_body_bytes` | int64 | `1048576` | `CUSTOS_SERVER__MAX_BODY_BYTES` | Max request body size in bytes |
| `server.read_header_timeout` | duration | `5s` | `CUSTOS_SERVER__READ_HEADER_TIMEOUT` | Max time to read request headers |
| `server.read_timeout` | duration | `30s` | `CUSTOS_SERVER__READ_TIMEOUT` | Max time to read the whole request |
| `server.request_timeout` | duration | `30s` | `CUSTOS_SERVER__REQUEST_TIMEOUT` | Per-request deadline |
| `server.shutdown_timeout` | duration | `20s` | `CUSTOS_SERVER__SHUTDOWN_TIMEOUT` | Graceful drain budget on shutdown |
| `server.tls` | section | `` | `CUSTOS_SERVER__TLS` | TLS settings for the API listener |
| `server.tls.cert_file` | string | `` | `CUSTOS_SERVER__TLS__CERT_FILE` | PEM certificate chain file (required when mode=required) |
| `server.tls.client_ca_file` | string | `` | `CUSTOS_SERVER__TLS__CLIENT_CA_FILE` | PEM CA file for verifying client certificates |
| `server.tls.key_file` | string | `` | `CUSTOS_SERVER__TLS__KEY_FILE` | PEM private key file (required when mode=required) |
| `server.tls.mode` | string | `required` | `CUSTOS_SERVER__TLS__MODE` | required (default) \| disabled (dev only) \| upstream |
| `server.tls.require_client_cert` | bool | `false` | `CUSTOS_SERVER__TLS__REQUIRE_CLIENT_CERT` | Require and verify client certificates (mTLS) |
| `server.trusted_proxies` | slice | `` | `CUSTOS_SERVER__TRUSTED_PROXIES` | CIDRs whose X-Forwarded-For values are trusted |
| `server.write_timeout` | duration | `1m0s` | `CUSTOS_SERVER__WRITE_TIMEOUT` | Max time to write the response |
| `slurm` | section | `` | `CUSTOS_SLURM` | Slurm adapter defaults (docs/slurm.md) |
| `slurm.dial_policy` | section | `` | `CUSTOS_SLURM__DIAL_POLICY` | SSRF policy enforced when dialing slurmrestd |
| `slurm.dial_policy.allow_http` | bool | `false` | `CUSTOS_SLURM__DIAL_POLICY__ALLOW_HTTP` | Permit plaintext http endpoints (dev only; loopback required) |
| `slurm.dial_policy.allow_loopback` | bool | `false` | `CUSTOS_SLURM__DIAL_POLICY__ALLOW_LOOPBACK` | Permit loopback endpoints (dev only) |
| `slurm.dial_policy.allow_private` | bool | `false` | `CUSTOS_SLURM__DIAL_POLICY__ALLOW_PRIVATE` | Permit RFC1918/ULA slurmrestd endpoints |
| `slurm.dial_policy.deny_cidrs` | slice | `` | `CUSTOS_SLURM__DIAL_POLICY__DENY_CIDRS` | Additional CIDRs always denied |
| `telemetry` | section | `` | `CUSTOS_TELEMETRY` | OpenTelemetry export |
| `telemetry.endpoint` | string | `` | `CUSTOS_TELEMETRY__ENDPOINT` | OTLP collector endpoint (required when exporter=otlp) |
| `telemetry.exporter` | string | `none` | `CUSTOS_TELEMETRY__EXPORTER` | none\|otlp |
| `telemetry.insecure` | bool | `false` | `CUSTOS_TELEMETRY__INSECURE` | Plaintext OTLP (dev_mode only) |
| `telemetry.sample_ratio` | float64 | `0.1` | `CUSTOS_TELEMETRY__SAMPLE_RATIO` | Trace sampling ratio in [0,1] |
| `telemetry.service_name` | string | `custos` | `CUSTOS_TELEMETRY__SERVICE_NAME` | service.name resource attribute |
| `validation` | section | `` | `CUSTOS_VALIDATION` | Script-validation pipeline and ShellCheck sidecar (docs/script-validation.md) |
| `validation.shellcheck` | section | `` | `CUSTOS_VALIDATION__SHELLCHECK` | ShellCheck sidecar client |
| `validation.shellcheck.enabled` | bool | `true` | `CUSTOS_VALIDATION__SHELLCHECK__ENABLED` | Enable the ShellCheck validator; false requires dev_mode |
| `validation.shellcheck.endpoint` | string | `http://127.0.0.1:8481` | `CUSTOS_VALIDATION__SHELLCHECK__ENDPOINT` | Sidecar base URL; loopback only |
| `validation.shellcheck.timeout` | duration | `10s` | `CUSTOS_VALIDATION__SHELLCHECK__TIMEOUT` | Per-request timeout |
| `validation.unavailable_severity` | string | `ERROR` | `CUSTOS_VALIDATION__UNAVAILABLE_SEVERITY` | Severity for CUSTOS900 when a validator is down; WARNING requires dev_mode |
| `worker` | section | `` | `CUSTOS_WORKER` | Work-queue lease loop |
| `worker.cluster_sync_interval` | duration | `1m0s` | `CUSTOS_WORKER__CLUSTER_SYNC_INTERVAL` | Base interval between cluster.sync runs per cluster |
| `worker.default_concurrency` | int | `4` | `CUSTOS_WORKER__DEFAULT_CONCURRENCY` | Default per-kind handler concurrency |
| `worker.heartbeat_interval` | duration | `10s` | `CUSTOS_WORKER__HEARTBEAT_INTERVAL` | Lease heartbeat cadence |
| `worker.kinds` | map<string,kind_limits> | `` | `CUSTOS_WORKER__KINDS (unsupported)` | Per-kind limits keyed by work-item kind |
| `worker.lease_duration` | duration | `30s` | `CUSTOS_WORKER__LEASE_DURATION` | Duration of a work-item lease |
| `worker.poll_interval` | duration | `1s` | `CUSTOS_WORKER__POLL_INTERVAL` | Delay between empty lease polls |
| `worker.shutdown_timeout` | duration | `20s` | `CUSTOS_WORKER__SHUTDOWN_TIMEOUT` | Drain budget for in-flight handlers |
